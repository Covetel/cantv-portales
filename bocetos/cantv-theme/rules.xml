<?xml version="1.0" encoding="UTF-8"?>
<rules
    xmlns="http://namespaces.plone.org/diazo"
    xmlns:css="http://namespaces.plone.org/diazo/css"
    xmlns:xsl="http://www.w3.org/1999/XSL/Transform">

    <!-- Apply the following rules to standard Plone pages -->
    <rules css:if-content="#visual-portal-wrapper">

        <theme href="index.html" />

        <!-- Copy base tag from Plone -->
        <before css:theme-children="html head" css:content="html head base" />

        <!-- Copy link, style, meta tags from Plone head -->
        <after css:theme-children="html head"
               content="/html/head/link | /html/head/style | /html/head/meta" />

        <!-- merge classes from Plone body with theme body -->
        <merge attributes="class" css:theme="body" css:content="body" />

        <!-- Copy script tags from Plone head to end of body -->
        <after css:theme-children="html body" css:content="html head script" />

        <!-- Insert your own rules here -->
                
        <!-- Reemplazo la etiqueta menu por la barra de navegación-->
        <replace css:content="#portal-globalnav" css:theme-children="#menu" />
        <!-- Inserto el menu de autenticación en el div mapa -->
        <after css:content="#portal-personaltools" css:theme="#menu" />
        
        <!-- Agregando buscador -->
        <after css:content="#portal-searchbox" css:theme="#map" />

        <!-- Elimino el breadcrumbs del contenido -->
        <drop css:content="#portal-column-content #viewlet-above-content" />
        <!-- Reemplazo la etiqueta med por el contenido del sitio -->
        <after css:content="#portal-column-one" css:theme-children="#med-top" />
        <after css:content="#portal-column-content" css:theme-children="#med-med" />
        <after css:content="#portal-column-two" css:theme-children="#med-bottom" />

        <!-- Elimino el texto del breadcrumbs -->
        <drop css:content="#portal-breadcrumbs #breadcrumbs-you-are-here" />
        <!-- Reemplazo la etiqueta mapa por el breadcrumbs -->
        <replace css:content="#portal-breadcrumbs" css:theme-children="#bottom" />
        

    </rules>

</rules>
